# Loaded only when the admin app is launched via `npm run admin`
# (vite --mode admin). Switches the bundle to the admin dashboard, which is
# never included in the public courier app.
VITE_ADMIN=true
