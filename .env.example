# ---------------------------------------------------------------------------
# Root environment template.
# Copy to `.env` and adjust. Each workspace also has its own `.env.example`.
#   cp .env.example .env
#   cp backend/.env.example backend/.env
#   cp frontend/.env.example frontend/.env
# ---------------------------------------------------------------------------

# Shared port configuration (kept in sync with the workspace .env files)
API_PORT=4000
WEB_PORT=5173
